## Setup
1. Install dependencies with `pip install -r requirements.txt`
2. Rename `.env.template` and paste the TMDB API Key I sent yall...
3. Run the application with `streamlit run app.py`

If you're using venv/conda, don't forget to activate it first :)

